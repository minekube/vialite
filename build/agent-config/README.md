# Native Image Agent Config

Native-image reflection/resource metadata belongs here once the bridge is
exercised with the GraalVM tracing agent.

The initial scaffold keeps this directory present so Docker and CI paths
are stable before the first full native capture.
